# Copy to .env and fill in. .env is gitignored — never commit real keys.

# TfL API app key (optional). Without it, requests still work at a lower rate
# limit. Get one at https://api-portal.tfl.gov.uk/.
TFL_APP_KEY=
