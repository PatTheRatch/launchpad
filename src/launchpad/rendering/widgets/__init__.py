"""Widgets: self-contained drawers for individual dashboard sections.

Each widget knows how to draw exactly one model into a region. Renderers
compose widgets to build a full layout, which keeps section drawing reusable
across portrait and landscape.
"""
