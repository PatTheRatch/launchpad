"""Domain models for Launchpad.

Models are plain dataclasses with no I/O and no rendering concerns. They are
the contract between data services (which produce them) and renderers (which
consume them).
"""
